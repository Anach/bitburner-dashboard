/**
 * Player-owned native BBS applications.
 *
 * The public BBS Connect plugin deliberately includes no native games. Players may add local
 * cabinet adapters here for software they have installed and maintain themselves.
 */
export function createBbsConnectUserApplications() {
    return [];
}
