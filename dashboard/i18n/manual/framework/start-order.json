{
    "manual": "Controls the priority order the Integration Service Supervisor uses when available Home RAM can't start every eligible service at once - move services up or down to decide which ones win limited capacity first.\n\nStrict Start Order defaults On. When the first eligible stopped service is blocked by available Home RAM, the Transient RAM Reserve, or the Service Startup RAM Limit, lower entries wait behind it until a later supervisor pass. Turn it Off to restore opportunistic admission, where a blocked service is skipped and smaller lower-priority services may start first. Capability-ineligible and Autostart-disabled entries do not block the queue. Autostart-disabled rows are greyed out and labelled as skipped, but remain reorderable so their saved position is ready if Autostart is enabled later.\n\nEach row shows the service entry script's own RAM and, when declared, a separate Children figure. Child costs come from the integration's placement-independent capacity list, so a worker remains visible after moving from Home to another rooted RAM host without ordinary payload scripts being mistaken for fixed children.\n\nThe Combined total figure above the list is a capacity-planning ceiling, not a live snapshot: every listed service plus its declared children, summed, regardless of whether each one (or an optional sub-toggle inside it) is currently switched on or where a child will run. This is different from what's actually consuming Home RAM right now - see Home Server's own Service Startup RAM Limit for that."
}
